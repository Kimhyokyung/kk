<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="talker">
	<select id="selectByEmail" parameterType="string" resultType="TalkerVo">
	<![CDATA[
		select email
		from tb_talker
	    where email = #{email}
	]]>
	</select>
	
	<insert id="insert" parameterType="TalkerVo">
		<![CDATA[		
		insert into tb_talker (EMAIL, PASSWORD, NICKNAME, STRESS_DEGREE, CONSULTING_TOPIC)
		     values ( #{email}, #{password }, #{nickname}, #{stress_degree}, #{consulting_topic} )	
		]]>
	</insert>
	
	<select id="selectByNoAndPassword" parameterType="map" resultType="TalkerVo">
		<![CDATA[
		select IDX, EMAIL, NICKNAME
		  from tb_talker
		 where email = #{email }
		   and password = #{password }
		]]>
	</select>
	
	<select id="selectListenerList" parameterType="map" resultType="map">
	<![CDATA[
	select IDX, EMAIL, NICKNAME, PASSWORD, CONSULTING_TOPIC from tb_listener;
		]]>
	</select>
	
</mapper>
